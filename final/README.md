Usage
-----

Run the CLI from the repository root using the source folder on PYTHONPATH:

```bash
PYTHONPATH=src python -m final list
PYTHONPATH=src python -m final add "Title" "Description"
```

The tasks are stored in `src/final/tasks.json`.

